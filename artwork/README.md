# Artwork

Artwork is kept conceptually separate from firmware.

The current production visual target is the **320×320 circular-safe ConsoleBadger artwork inside the 360×360 display canvas**.

Generated video/CBV build artifacts are treated as release assets rather than firmware source. The working CBV filename used by the current firmware is:

`ConsoleBadger_320_CIRCULAR_SAFE_CROPPED_DIRECTDMA_HC9.CBV`
